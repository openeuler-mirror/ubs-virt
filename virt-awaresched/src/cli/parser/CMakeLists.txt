file(GLOB_RECURSE SRC_FILES *.cpp)
FIND_INCLUDE_DIR(cli_parse_INCLUDE ${CMAKE_CURRENT_SOURCE_DIR})
add_library(cli_parse STATIC ${SRC_FILES})
target_include_directories(cli_parse PUBLIC ${cli_parse_INCLUDE})
