file(GLOB LOOPER_CPP "*.cpp")
FIND_INCLUDE_DIR(LOOPER_INCLUDE ${CMAKE_CURRENT_SOURCE_DIR})
add_library(looper STATIC ${LOOPER_CPP})
target_include_directories(looper PUBLIC ${LOOPER_INCLUDE})
target_link_libraries(looper PUBLIC vas_daemon_register acquire cluster_sched socket_server api logger)
